---
import {getCollection} from "astro:content";
import Layout from '../layouts/Layout.astro';
import H1 from "../components/H1.astro";
import Main from "../components/Main.astro";
import MovieList from "../components/MovieList.astro"

const movies = await getCollection("movies");
const lastMovieEntries = movies.sort((a,b)=> new Date(b.data.date)-new Date(a.data.date)).slice(0,5);
---
<Layout title="Semanarama">
    <Main>
        <H1 text="Últimas entradas"/>
        <MovieList movies={lastMovieEntries}/>
    </Main>
</Layout>
